import styled from "styled-components";

const HeaderContainer = styled.div`
  padding: 32px 0;
`;

export const Header = () => (
  <HeaderContainer>
    <h1>Formula1 drivers</h1>
  </HeaderContainer>
);
